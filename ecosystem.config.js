module.exports = {
  apps: [
    {
      name: "claude-terminal-blue",
      script: "server.js",
      cwd: __dirname,
      env: {
        NODE_ENV: "production",
        PORT: 3000,
      },
      autorestart: true,
      max_restarts: 10,
      max_memory_restart: "1G",
      error_file: "./logs/error-blue.log",
      out_file: "./logs/out-blue.log",
      merge_logs: true,
      log_date_format: "YYYY-MM-DD HH:mm:ss",
      watch: false,
      kill_timeout: 10000,
      listen_timeout: 60000,
      wait_ready: true,
    },
    {
      name: "claude-terminal-green",
      script: "server.js",
      cwd: __dirname,
      env: {
        NODE_ENV: "production",
        PORT: 3001,
      },
      autorestart: true,
      max_restarts: 10,
      max_memory_restart: "1G",
      error_file: "./logs/error-green.log",
      out_file: "./logs/out-green.log",
      merge_logs: true,
      log_date_format: "YYYY-MM-DD HH:mm:ss",
      watch: false,
      kill_timeout: 10000,
      listen_timeout: 60000,
      wait_ready: true,
    },
  ],
};
